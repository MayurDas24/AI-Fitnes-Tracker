# Frontend environment variables
# Copy this file to .env and customize for local/dev/prod usage.

# Backend API base URL
VITE_API_URL=http://localhost:5000/api

# Optional: only needed if frontend calls AI provider directly.
# Prefer storing secrets in backend/.env when possible.
VITE_GROQ_API_KEY=your-groq-api-key
